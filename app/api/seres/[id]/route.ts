import { NextRequest,NextResponse } from "next/server";import { isValidObjectId } from "mongoose";import { connectDB } from "@/lib/db";import { requireAdmin,requireUser } from "@/lib/auth";import { validateSer } from "@/lib/validation";import { Ser } from "@/models/Ser";import { User } from "@/models/User";
type Context={params:Promise<{id:string}>};
export async function GET(_:NextRequest,{params}:Context){if(!await requireUser())return NextResponse.json({message:"Debes iniciar sesión para consultar esta ficha."},{status:401});await connectDB();const{id}=await params;if(!isValidObjectId(id))return NextResponse.json({message:"Identificador inválido"},{status:400});const ser=await Ser.findById(id).lean();return ser?NextResponse.json(ser):NextResponse.json({message:"Ser no encontrado"},{status:404})}
export async function PUT(request:NextRequest,{params}:Context){if(!await requireAdmin())return NextResponse.json({message:"No autorizado"},{status:401});await connectDB();const{id}=await params;const ser=await Ser.findByIdAndUpdate(id,validateSer(await request.json()),{new:true,runValidators:true});return ser?NextResponse.json(ser):NextResponse.json({message:"Ser no encontrado"},{status:404})}
export async function DELETE(_:NextRequest,{params}:Context){if(!await requireAdmin())return NextResponse.json({message:"No autorizado"},{status:401});await connectDB();const{id}=await params;const ser=await Ser.findByIdAndDelete(id);if(ser)await User.updateMany({},{$pull:{favoritos:id}});return ser?NextResponse.json({message:"Ser eliminado"}):NextResponse.json({message:"Ser no encontrado"},{status:404})}
